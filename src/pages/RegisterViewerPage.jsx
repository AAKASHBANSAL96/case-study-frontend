import { useState } from "react";
import AdminAccessRequired from "../components/AdminAccessRequired";
import AppLink from "../components/AppLink";
import { request } from "../services/api";
export default function RegisterViewerPage({ session }) { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); if (session.user?.role !== "admin") return <AdminAccessRequired />; async function submit(event) { event.preventDefault(); try { const user = await request("/auth/users", { method: "POST", body: JSON.stringify({ email, password, role: "viewer" }) }, session.token); setMessage(`Viewer account created: ${user.email}`); setEmail(""); setPassword(""); } catch (requestError) { setMessage(requestError.message); } } return <main className="adminPage"><section><p>HEALTHCHECK OPS / ADMIN</p><h1>Register viewer</h1><span>Viewers can use the Viewer Login page to access logs and statistics.</span><form onSubmit={submit}><input type="email" required placeholder="Viewer email" value={email} onChange={(event) => setEmail(event.target.value)} /><input type="password" required minLength="8" placeholder="Temporary password" value={password} onChange={(event) => setPassword(event.target.value)} /><button>Create viewer account</button></form>{message && <div className="message">{message}</div>}<AppLink href="/">Back to dashboard</AppLink></section></main>; }
